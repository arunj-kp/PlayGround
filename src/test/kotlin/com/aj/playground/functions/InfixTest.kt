package com.aj.playground.functions

import org.junit.Assert.*
import org.junit.Before
import org.junit.Test

class InfixTest {
    @Test
    fun `when two strings with same value are evaluated, it should be equal`() {
        val string1 = "arun"
        string1 thullyam "arun"
    }
}